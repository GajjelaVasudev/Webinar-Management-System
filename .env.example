# Django Settings
DEBUG=False
SECRET_KEY=your-django-secret-key-here
ALLOWED_HOSTS=localhost,127.0.0.1,your-render-service.onrender.com

# CORS Settings (for Vercel frontend)
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173,https://your-vercel-project.vercel.app

# Database (PostgreSQL Configuration)
DB_NAME=webinar_db
DB_USER=postgres
DB_PASSWORD=your_database_password
DB_HOST=localhost
DB_PORT=5432
# Alternative: Use DATABASE_URL format (some platforms like Render use this)
# DATABASE_URL=postgresql://user:password@localhost/dbname

# Optional default admin bootstrap (used on Render where shell access is limited)
# DEFAULT_ADMIN_USERNAME=admin
# DEFAULT_ADMIN_PASSWORD=changeme
# DEFAULT_ADMIN_EMAIL=admin@example.com
